/* eslint-env node, mocha */
const assert = require('chai').assert;

describe('mocha test harness', function() {
  it('should be able to run a test', function() {
    assert(true, 'it should have run a test and one assertion');
  });
});

